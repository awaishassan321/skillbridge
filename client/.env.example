# Base URL of the deployed backend API (no trailing slash).
# CRA inlines this at build time — set it in your hosting provider's
# build environment before running `npm run build`.
REACT_APP_API_URL=http://localhost:5010
